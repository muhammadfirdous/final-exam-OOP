# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/subscription_db
spring.datasource.username=root
spring.datasource.password=Ucakg@24
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Logging SQL Queries for Debugging
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Optional: Change the server port if 8080 is already in use
server.port=8080
dependencies {
Add H2 database dependency
implementation 'com.h2database:h2'
// Other dependencies...
}
